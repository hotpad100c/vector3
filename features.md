# Vector3 功能一览

Vector3 是 [Flashback](https://modrinth.com/mod/flashback) 的扩展：在回放时间轴上用关键帧制作图形、文字、媒体和世界片段的动画，并补充一批相机与时间轴编辑工具。

> 前置：Minecraft 26.3， Fabric Loader， Fabric API， Flashback，RyansRenderingKit
>
> 兼容：Sodium，Iris

## 图形

- **Shape 轨道**：新增时间轴轨道。关键帧记录图形的位置、旋转、缩放、颜色、可见性等，并在关键帧之间插值。
- **图形类型**：
  - 几何体：长方体、球体、圆面、圆柱、圆锥。
  - 线条类：线段、折线、箭头。
  - 游戏内容：方块（可设方块属性）、物品、实体、OBJ 模型。
  - 其他：文字、图片、视频（起始时间、手动/自动播放、循环，可带音频）。
- **线框与外观**：可叠加线框，线框可单独着色，也可只显示线框。支持透视显示（see-through）和发光描边。
- **父子层级**：图形可以挂到另一个图形下，随父级移动、旋转、缩放。更换父级时自动换算，保持图形在世界中的位置不变。在 Shape Manager 里把图形拖到另一个图形上即可建立父子关系，拖到空白处解除，所有关键帧会按各自时刻的父级姿态一起换算。
- **挂载实体**：图形可以挂到实体上，以脚底、身体或眼睛为挂载点（同 Track Entity），可选是否跟随实体朝向；图形自身的变换作为挂载点上的偏移。实体不在时图形连同子图形一起隐藏。挂载和父图形二选一。
- **文字**：支持 JSON 或 `§` 格式的颜色与样式。可使用自定义 `.ttf`/`.otf` 字体，以距离场方式清晰渲染；可设阴影、描边和朝向镜头的模式。
- **文字过渡**：两个关键帧之间的文字只擦除并重打第一处不同之后的部分，相同的文字和格式保留。
- **文字辉光**：自定义字体的文字可以开启真正的泛光（bloom），轮廓也可以单独发光。强度、扩散范围和颜色可调，并按关键帧插值；颜色默认跟随文字。
- **删除即清理**：图形的关键帧从所有轨道删掉，或所在轨道被禁用后，图形会从世界中移除。

## 区域图形（AreaShape）

- **搬运一块世界**：把世界里一块区域烘焙成一个整体，放到别处并可移动、旋转、缩放、淡入淡出。原位置的方块会被隐藏。
- **完整内容**：方块实体一并跟随。可选投影区域内的实体和粒子，它们跟着区域一起变换，也一起变透明。
- **半透明**：透明度直接写进顶点色，开着光影也有效。区域网格在原版主渲染流程里绘制，和实体、粒子的前后遮挡关系正确。
- **实时同步**：源区域内的方块变化会自动重新烘焙，不会闪现被隐藏的方块。
- **父子层级**：区域作为子图形时，投影跟随父级变换；蓝色的源区域框保持在原处不动。

## 视口操纵器（Gizmo）

- **四种模式**：移动（G）、旋转（R）、缩放（B）、几何（M）。几何模式用于调整尺寸、半径、线段端点、区域角点等。
- **坐标系**：可切换全局或局部。全局沿世界轴操作；局部沿图形自身的朝向（包含父级）操作。
- **吸附**：按住 Ctrl 吸附，位置和尺寸按 0.5 格，旋转按 15°。
- **点选编辑**：在视口里右键点击图形，直接跳到它最近的关键帧进行编辑。
- **自动关键帧**：在 Shape Manager 开启后，没有选中关键帧时点选图形会直接编辑播放头处的状态，移动后在播放头处自动建立关键帧。
- **复制与删除图形**：鼠标在视口或 Shape Manager 上时，Ctrl+D 把选中关键帧所属的图形连同全部关键帧复制到新轨道（新 ID，名称加“副本”，一起复制的父子关系会指向副本）；Del 把这些图形连同它们在所有轨道上的关键帧一起删除。都可撤销。在时间轴上按 Del 仍只删除选中的关键帧。
- **放到指着的位置**：选中图形后中键点击，把它移到鼠标指着的方块表面或实体上；按住 Ctrl 对齐到方块格中心。

## 相机

- **编辑器相机**：环绕、平移、缩放查看场景，按 F 聚焦鼠标所指的对象。
- **相机关键帧操纵器**：选中 Flashback 的相机关键帧后，视口里的机位处出现 X/Y/Z 移动轴、自由移动点，以及偏航、俯仰、滚转三个旋转环，并用白线表示朝向、绿线表示上方。按住 Ctrl 吸附到 0.5 格和 15°。
- **环绕关键帧增强**：Flashback 的环绕（Orbit）关键帧可额外设置倾斜，并可在视口里用操纵器编辑中心、距离和角度。
- **注视（Look To）关键帧**：两个 Look To 关键帧之间的作用域内，镜头强制看向目标，其它相机参数不受影响。目标可以是实体、坐标或图形；实体可选瞄准眼睛、中心或脚底；相邻关键帧之间目标会平滑过渡。
- **希区柯克变焦（Dolly Zoom）关键帧**：相机沿视线推拉的同时自动调整 FOV，让目标在画面中的大小保持不变。目标可以是实体、坐标或图形，并实时跟随；可一键从当前相机取值。
- **相机抖动增强**：Flashback 的相机抖动关键帧新增滚转、位置抖动（沿相机自身轴，单位米）、多层分形噪声（层数、粗糙度）和种子，并附手持、走路、车载、冲击预设。抖动相位按时间轴积分得出，同一帧无论播放、拖动、暂停还是导出都完全一致；默认参数下与原版效果相同。
- **跟踪实体**：Flashback 的 Track Entity 关键帧可以从下拉列表选实体，不用手输 UUID。
- **吸管**：实体和图形的选择框旁都有“吸取”按钮，直接在视口里点选目标。

## 时间轴

- **速度曲线**：插值类型里多了“自定义”。选中后属性窗口出现“速度曲线”分页，可以用多点贝塞尔曲线自由编排本关键帧到下一个关键帧之间的时间进度（可超出 0~1 做回弹、预备动作），附带线性、缓入、缓出、缓入缓出、回弹预设；拖动时实时预览，可撤销，随存档保存。
- **跳过（Skip）关键帧**：两个 Skip 关键帧之间的片段在播放和导出时完全跳过。最多只能有一条 Skip 轨道。
- **分组**：
  - 把选中的若干关键帧建成一组。可以在轨道菜单里建组，也可以在空白处右键选中区域建组。
  - 分组以彩色条显示。拖动彩色条或组内空白处会整组移动，关键帧实时跟随。
  - 右键分组可以重命名、另存为预制、解散或删除。
  - 撤销、重做、保存和重新打开回放后，分组都会保留。
- **跨轨道拖动**：
  - 关键帧和分组都可以拖到同类型的其他轨道上。拖到最后一条轨道下方会自动新建同类轨道。
  - 目标不合法时（类型不同、越界、放不下）预览显示为红色，松手后只做水平移动。
- **Alt 拖动复制**（同 DaVinci Resolve）：
  - 按住 Alt 拖动关键帧（单个或多选），松手处放下副本，原关键帧不动；也可以跨轨道复制，拖到最下方会新建轨道。
  - 按住 Alt 拖动分组，在目标位置生成一个新的分组副本。
  - 按住 Alt 拖动轨道把手，原位留下整条轨道的副本（包括名称、颜色、启用状态），拖走的是原轨道。
  - Flashback 原来按住 Alt 拖动是以支点缩放关键帧，现在改为 Ctrl+Alt。
- **批量编辑**：
  - 选中多个关键帧时，属性窗口按类型分区显示，所有类型都支持。图形分为“图形”公共区（各类型共有的位置、旋转、颜色等）和各类型专属区。
  - 同一栏的值不一致时显示“-”并锁定；按住 Ctrl 解锁后修改，会写入所有目标，且只改动的那一项（向量按分量）会变化。值一致的栏可以直接修改。插值类型同样支持。
  - 视口 Gizmo 对选中的全部图形生效：移动为整体平移；旋转、缩放在全局空间下围绕共同中心，在局部空间下各自围绕自身中心。父子同时选中时子级不会重复变换。中键放置会整组平移。
  - 每次修改合并为一条历史记录，可一次撤销。
- **属性复制粘贴**：
  - 属性窗口里 Ctrl+右键点击条目可以选中或取消，Ctrl+右键拖动可以框选。Esc 清空选择。
  - Ctrl+C 复制所选条目的值，Ctrl+V 粘贴到当前关键帧（多选时粘贴到全部选中的关键帧）里同名的条目，也可以在空白处或已选条目上右键打开菜单操作。
  - 按条目名称匹配，所以图形之间可以跨类型粘贴位置、颜色等；找不到对应条目的会跳过，并提示粘贴了几项。
  - 剪贴板保存在 `config/vector3/property_clipboard.json`，关闭或切换回放后仍可使用。
- **即时预览**：在 Shape Manager 中开启后，拖动关键帧时世界里的效果实时变化，不用等松手。
- **修正**：Flashback 框选关键帧时范围整体偏移的问题已修正；右键空白处不会再清空当前选区。

## 片段（Clips）

- **Clips 窗口**：列出回放文件夹里的所有回放及时长，可以直接拖到时间轴上。
- **片段轨道**：拖入的回放成为时间轴顶部“片段”轨道上的一个长条。第一次往普通回放里加片段时，回放本身会自动成为第一个片段。
- **拼接**：在片段轨道上拖动片段调整先后顺序，在属性窗口设置入点和出点，然后在 Clips 窗口点击“应用片段更改”。回放会退出、在后台按顺序首尾合成，再自动重新打开。
  - 其他轨道上的关键帧会跟随所在的片段一起移动；编辑器状态保持不变。
  - 合成以 Flashback 的录制分段为单位，入点、出点之外的部分会像跳过关键帧一样在播放和导出时被跳过。
  - 片段源文件会复制一份保存在 Flashback 数据目录下，原回放移走或删除不影响项目。
  - 不同游戏版本录制的回放不能拼接，拖入时会提示。
- **空项目**：回放列表右上角的“空项目”按钮会打开一个只有虚空的新项目。第一次使用时会自动创建一个临时的虚空世界录制模板，录完即删除。

## 预制（Prefab）

- **预制篮**：窗口里列出内置模板和自己保存的预制，拖进时间轴即可放置。
- **放置模式**：在视口里用操纵器设定预制的中心、旋转、缩放，可以同时调整时长倍率，路径会实时预览。确认后生成新的轨道，整次放置可一步撤销。
- **内置模板**：环绕、希区柯克变焦，模板参数可调。
- **保存预制**：选中的关键帧或一个分组可以存成预制。坐标类数据（相机位置、环绕中心、注视目标、图形）会换算成相对预制原点的值。放置时图形会生成新的 ID，同一个预制放两次得到两组独立的图形。
- **重新编辑**：放置后的预制分组可以重新进入放置模式，调整变换后替换原来的内容。

## 界面

- **窗口菜单**：Flashback 顶栏上的窗口开关合并为一个"窗口"下拉菜单，其中也包括 Shape Manager 和预制篮。
- **图形管理器（Shape Manager）**：以树状结构列出所有图形，支持搜索、命名和父子层级，并提供编辑器模式、即时预览等开关。
- **属性窗口**：Flashback 的关键帧编辑弹窗改成可停靠的“属性”窗口，跟随选中的关键帧显示，不再遮挡视口；其它弹窗可以按住空白处拖动。
- **窗口记忆**：Shape Manager 和预制篮与 Flashback 自带窗口一样，重进游戏后保持打开状态和停靠位置。
- **图标**：自定义轨道和各图形类型都有专属图标。

## 兼容性

- **Iris 光影**：物品、方块、实体、图片、视频、文字等原版内容可以选择绕过光影单独绘制，避免被光影包弄花。Replay Visuals 关闭天空时，光影包的天空也会一起隐藏。
- **Sodium**：区域图形的隐藏与烘焙适配了 Sodium 的区块构建和方块渲染。
