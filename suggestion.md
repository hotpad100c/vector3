# Vector3 功能建议

对照主流剪辑和动画软件，整理 Vector3（加上 Flashback 本身）还欠缺、或可以更顺手的地方。参考的软件有：Premiere Pro、DaVinci Resolve、After Effects、Blender、Unity Timeline、Cinema 4D，以及 Minecraft 圈子里的 Replay Mod。

**优先级**：
- **P1**：收益大，实现上基本可行，建议优先做。
- **P2**：有价值，但工作量中等。
- **P3**：锦上添花，或者需要较大的架构调整。

每条后面括号里注明参考来源。

---

## 一、关键帧与动画曲线

### P1
- **图表编辑器（Graph Editor）**（AE、Blender、Resolve Fusion）
  - 现在的"速度曲线"只能控制两个关键帧之间的**时间进度**。主流软件还有**数值曲线**：把位置 X、旋转 Y 这类单个通道的数值随时间画成曲线，可以直接拖动曲线上的点和手柄。
  - 可以作为属性窗口的新分页，复用 `SpeedCurveEditor` 的画布代码。按通道显示时，需要从关键帧中抽样出各通道的值。
- **一键缓动**（AE 的 F9 Easy Ease、Resolve 的 Ease In/Out）
  - 在时间轴上右键选中的关键帧，直接套用"缓入 / 缓出 / 缓入缓出 / 线性 / 保持"。现在要先打开属性窗口、再选插值类型，步骤较多。
- **运动路径可视化**（AE Motion Path、Blender Motion Paths）
  - 在视口里用线画出相机和图形的运动轨迹，每隔 N tick 标一个点，关键帧处用大点表示。点击路径上的关键帧点可以直接选中它。Flashback 自带的相机路径显示比较简单，可以在它的基础上增强。
- **时间轴吸附**（Premiere 的 S 键）
  - 拖动关键帧或分组时，吸附到播放头、其他关键帧、标记和分组边缘，并显示吸附线。
- **关键帧均匀分布 / 时间反转**（AE 的 Time-Reverse Keyframes、Blender 的 Distribute）
  - 选中若干关键帧后，让它们在首尾之间等距分布；或者把这段动画倒过来播放。
- **标记（Marker）**（Resolve、Premiere）
  - 在时间轴上打带颜色和备注的标记，比如"这里切镜头""这里接音乐鼓点"。支持跳到上一个 / 下一个标记，也可以作为吸附目标。

### P2
- **循环与往返**（AE 的 loopOut、Blender 的 Cycles 修改器）
  - 让一段关键帧在结束后自动重复，或者来回往返，适合旋转的招牌、呼吸灯效果。可以做成轨道或分组的属性。
- **通用摆动（Wiggle）**（AE 的 wiggle、Blender 的 Noise 修改器）
  - 相机抖动已经有分形噪声了。可以把同样的噪声开放给任意数值属性，比如图形位置微微浮动、灯光闪烁。
- **匀速漫游（Roving Keyframes）**（AE）
  - 让中间的关键帧自动调整时间，使整段路径保持匀速运动。相机穿行镜头特别需要。
- **按属性分别打关键帧**（AE 秒表、Resolve 的菱形按钮）
  - 现在一个图形关键帧记录的是全部属性。主流做法是每个属性旁边有一个 ◆ 按钮，只给这个属性打关键帧，并带"跳到上一个 / 下一个关键帧"的箭头。
  - 这需要把 ShapeState 拆成多条通道，架构改动大。可以先做一个折中：关键帧上记录"哪些字段是有效的"，没勾选的字段插值时沿用相邻的关键帧。

### P3
- **驱动 / 表达式**（Blender Drivers、AE Expressions）
  - 让一个属性跟随另一个属性，比如"文字大小 = 与相机的距离 × 0.1"，或者跟随实体速度。
- **动作录制**（Replay Mod、Unity 的 Recorder）
  - 飞行视角下实时录制相机运动，自动生成关键帧，并带简化（抽稀）选项。

---

## 二、新的关键帧 / 图形类型

### P1
- **路径跟随（Follow Path）**（C4D 的 Align to Spline、Blender 的 Follow Path）
  - 用一条折线或曲线图形作为轨道，让相机或图形沿着它运动，进度可以打关键帧，还可选"朝向切线"和转弯时的倾斜。可以复用现有的 `line_strip` 图形作为路径。
- **转场 / 淡入淡出**（Premiere 的 Cross Dissolve、Dip to Black）
  - 全屏黑场、白场、颜色淡入淡出的关键帧，用来在镜头之间过渡。实现上相当于在最终画面上叠一层全屏色块，成本低、使用频率高。
- **镜头切换轨（多机位）**（Resolve Multicam、Unity 的 Cinemachine 镜头轨）
  - 预先定义多个"机位"（每个机位是一组相机关键帧，或一个固定机位），再用一条切换轨决定每一段时间用哪个机位。改剪辑节奏时不用重新摆相机。
- **音频可视化**（所有剪辑软件）
  - 在时间轴的音频轨上显示波形，支持音量包络（淡入淡出的关键帧），以及把鼓点标成标记用于吸附。

### P2
- **调色 / 画面效果关键帧**（Resolve 调色页、Premiere Lumetri）
  - 曝光、对比度、饱和度、色温、暗角、LUT，可以随时间变化。可以做成一个全屏后处理通道，和文字辉光的合成流程类似。
- **景深 / 对焦关键帧**（Resolve、Blender 的 Camera DOF）
  - 设置对焦距离和光圈，焦点也可以用吸管指定某个实体或图形并自动跟焦。原版下需要自己实现模糊；光影包下可以尝试驱动它的景深参数。
- **文字动画器**（AE 的 Text Animators）
  - 按字、按词做出场效果，比如逐字淡入、打字机、弹跳、逐字上色。现在的"文字过渡"只擦除并重打变化的部分，可以扩展成一组预设。
- **粒子发射器图形**
  - 在图形位置持续生成原版粒子，粒子种类、速率、扩散范围都可以打关键帧。
- **实体姿态关键帧**
  - 给盔甲架或假人实体设置头、身体、手、腿的角度，并在关键帧之间插值，用来摆拍。

### P3
- **遮罩 / 蒙版**（AE Mask、Resolve Power Window）
  - 用一个形状控制某个图形或画面效果只在区域内显示，可以做"揭示"类转场。
- **光源图形**
  - 带颜色和强度的点光源，配合光影包或自定义光照使用，实现难度较高。

---

## 三、时间轴界面

### P1
- **入点 / 出点与循环播放**（全部剪辑软件的 I / O 键）
  - 设定一段区间循环预览，反复调整同一段动画时不用手动拖回播放头。
- **播放快捷键 J / K / L**（Premiere、Resolve）
  - 倒放、暂停、快进，多按几次加速。逐帧查看用左右方向键。
- **轨道管理**（Premiere、Resolve）
  - 轨道锁定（防误改）、单独显示（Solo，只看这一条轨道的效果）、折叠，以及轨道文件夹（把多条轨道收进一个组）。
- **拖动时的数值提示**
  - 拖动关键帧时，在鼠标旁显示"+12 tick / 第 340 tick"，缩放一组关键帧时显示倍率。
- **时间码显示与跳转**
  - 在秒、tick、帧之间切换显示单位，并支持直接输入时间跳转。

### P2
- **波纹编辑（Ripple）**（Premiere、Resolve）
  - 在某处插入或删除一段时间时，后面所有关键帧自动整体平移，不用手动把后半段全部挪一遍。
- **历史面板**（Photoshop、Blender 的 Undo History）
  - 把撤销栈列成一个列表，点任意一条直接回到那一步。现有的历史记录描述已经比较完整，做一个窗口展示即可。
- **关键帧颜色标签**
  - 给关键帧染色、加小标签，多人协作或复杂镜头时更容易辨认。

---

## 四、属性面板（检查器）

### P1
- **拖动标签改值**（AE、Unity、Blender）
  - 按住数值字段的**标签**左右拖动就能改值，不用精确点中输入框。ImGui 的 drag 控件本身支持在框内拖动，但标签不响应。
- **重置为默认值**（Unity、Blender）
  - 右键某个条目，选"重置"。这正好可以复用刚做的条目识别机制：记录一个默认关键帧的采集结果，然后用重放写回。
- **表达式输入**（Blender、AE）
  - 数值框里可以输入 `=2*3`、`+0.5`（在当前值上加），批量调整时尤其方便。

### P2
- **属性预设**（AE Animation Presets、Unity Presets）
  - 把一组条目的值保存成有名字的预设（比如"标题字样式""警告框"），之后一键套用。可以基于现在的属性剪贴板扩展，改成多个具名的剪贴板。
- **属性搜索**
  - 属性窗口顶部加搜索框，按名称过滤条目。图形的条目越来越多，这个会越来越有用。

---

## 五、视口

### P1
- **相机预览小窗（画中画）**（Blender 的相机视图、Resolve 的 Viewer）
  - 用编辑器相机自由查看场景时，在角落显示"成片相机"此刻实际看到的画面。现在要确认构图，只能切回成片视角。
- **相机视锥可视化**
  - 在相机关键帧和环绕关键帧的机位上画出视锥（受 FOV 和画面比例影响），可以直接看出取景范围。
- **复制图形（Ctrl+D）**
  - 复制选中的图形，连同它的关键帧轨道一起复制，并立刻进入移动模式。

### P2
- **对齐与分布**（Figma、Blender）
  - 多个图形左对齐、居中对齐、等距分布。批量 Gizmo 已经有共同中心和整组变换的计算，可以在此基础上实现。
- **阵列 / 重复**（C4D Cloner、Blender Array）
  - 把一个图形按直线、圆形或网格重复 N 份，并可以让每份依次错开时间出场。
- **测量工具**
  - 在视口里量两点的距离和角度，方便精确摆放。

---

## 六、工作流与易用性

### P1
- **快捷键速查表**
  - 按 `?` 或从菜单打开一个面板，列出 G/R/B/M、Ctrl 吸附、Ctrl+右键选择、中键放置等所有快捷键。现在这些功能对新用户基本不可发现。
- **空状态引导**
  - 属性窗口、Shape Manager、预制篮在空的时候，给出下一步该做什么的提示，比如"在时间轴上右键新建 Shape 轨道"。
- **预制导出 / 导入**
  - 把预制导出成单个文件分享给别人，再从文件导入。

### P2
- **低分辨率预览导出**（Premiere 的代理、Resolve 的 Render in Place）
  - 用低分辨率、低帧率快速导出一版小样来检查节奏，不用每次都等全质量导出。
- **多段导出 / 渲染队列**（Premiere 的 Media Encoder、Resolve 的 Render Queue）
  - 一次排队导出多个区间或多种设置，比如竖屏和横屏各一版。
- **自定义快捷键**
  - vector3 的快捷键（G/R/B/M 等）加入 Flashback 的快捷键设置界面，可以重新绑定。

---

## 建议的实施顺序
1. **马上能提升手感的**：一键缓动、时间轴吸附、入点出点循环播放、拖动时的数值提示、快捷键速查表。
2. **补上核心能力的**：图表编辑器、运动路径可视化、路径跟随、转场、相机预览小窗。
3. **放大创作空间的**：镜头切换轨、调色和景深、文字动画器、阵列。
4. **长期架构项**：按属性分别打关键帧、驱动和表达式。
