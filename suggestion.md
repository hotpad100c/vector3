# Vector3 功能建议清单

对照 Premiere Pro、DaVinci Resolve、After Effects、Blender、Unity Timeline、Cinema 4D 和 Replay Mod 整理。括号里是参考来源。

**优先级**：P1 收益大、基本可行；P2 有价值、工作量中等；P3 锦上添花或需要较大架构调整。

---

## 一、关键帧与动画曲线

### P1
- [ ] **图表编辑器（Graph Editor）**（AE、Blender、Resolve Fusion）
  - 把位置 X、旋转 Y 这类单个通道的数值随时间画成曲线，直接拖动曲线上的点和手柄。
  - 作为属性窗口的新分页，复用 `SpeedCurveEditor` 的画布；按通道从关键帧中抽样取值。
- [ ] **一键缓动**（AE 的 F9 Easy Ease、Resolve 的 Ease In/Out）
  - 时间轴上右键选中的关键帧，直接套用缓入 / 缓出 / 缓入缓出 / 线性 / 保持。
- [ ] **运动路径可视化**（AE Motion Path、Blender Motion Paths）
  - 视口里画出相机和图形的轨迹，每隔 N tick 一个点，关键帧处用大点；点击关键帧点直接选中。
- [ ] **时间轴吸附**（Premiere 的 S 键）
  - 拖动关键帧或分组时吸附到播放头、其他关键帧、标记和分组边缘，并显示吸附线。
  - 已完成一部分：片段和音频的移动、裁剪会吸附到片段边缘和播放头。
- [ ] **关键帧均匀分布 / 时间反转**（AE Time-Reverse Keyframes、Blender Distribute）
  - 选中的关键帧在首尾之间等距分布，或把这段动画倒过来。
- [ ] **标记（Marker）**（Resolve、Premiere）
  - 带颜色和备注的时间轴标记，可跳到上一个 / 下一个，也作为吸附目标。

### P2
- [ ] **循环与往返**（AE loopOut、Blender Cycles 修改器）
  - 一段关键帧结束后自动重复或来回往返，做成轨道或分组的属性。
- [ ] **通用摆动（Wiggle）**（AE wiggle、Blender Noise 修改器）
  - 把相机抖动的分形噪声开放给任意数值属性，比如图形微微浮动、灯光闪烁。
- [ ] **匀速漫游（Roving Keyframes）**（AE）
  - 中间关键帧自动调整时间，让整段路径保持匀速，适合相机穿行镜头。
- [ ] **按属性分别打关键帧**（AE 秒表、Resolve 菱形按钮）
  - 每个属性旁一个 ◆ 按钮，只给这个属性打关键帧，带上一个 / 下一个关键帧箭头。
  - 折中方案：关键帧记录"哪些字段有效"，没勾选的字段插值时沿用相邻关键帧。

### P3
- [ ] **驱动 / 表达式**（Blender Drivers、AE Expressions）
  - 让一个属性跟随另一个属性或实体状态，比如"文字大小 = 与相机距离 × 0.1"。
- [ ] **动作录制**（Replay Mod、Unity Recorder）
  - 飞行视角下实时录制相机运动并生成关键帧，带抽稀选项。

---

## 二、新的关键帧 / 图形类型

### P1
- [ ] **路径跟随（Follow Path）**（C4D Align to Spline、Blender Follow Path）
  - 用 `line_strip` 图形作路径，相机或图形沿它运动；进度可打关键帧，可选朝向切线和转弯倾斜。
- [ ] **转场 / 淡入淡出**（Premiere Cross Dissolve、Dip to Black）
  - 全屏黑场、白场、颜色淡入淡出关键帧，在最终画面上叠一层全屏色块。
- [ ] **镜头切换轨（多机位）**（Resolve Multicam、Unity Cinemachine）
  - 预先定义多个机位，再用一条切换轨决定每段时间用哪个机位。
- [ ] **音频可视化与包络**（所有剪辑软件）
  - 音量包络（淡入淡出关键帧），以及把鼓点标成标记用于吸附。
  - 已完成一部分：Flashback 自带波形；vector3 加了音频裁剪、切分、单个关键帧的音量和音高、更换文件。

### P2
- [ ] **调色 / 画面效果关键帧**（Resolve 调色页、Premiere Lumetri）
  - 曝光、对比度、饱和度、色温、暗角、LUT 随时间变化，做成全屏后处理通道。
- [ ] **景深 / 对焦关键帧**（Resolve、Blender Camera DOF）
  - 对焦距离和光圈，可用吸管指定实体或图形自动跟焦；原版自己实现模糊，光影包下驱动其景深参数。
- [ ] **文字动画器**（AE Text Animators）
  - 逐字淡入、打字机、弹跳、逐字上色等出场预设，在现有"文字过渡"基础上扩展。
- [ ] **粒子发射器图形**
  - 在图形位置持续生成原版粒子，种类、速率、扩散范围可打关键帧。
- [ ] **实体姿态关键帧**
  - 盔甲架或假人的头、身体、手、腿角度，关键帧之间插值，用来摆拍。

### P3
- [ ] **遮罩 / 蒙版**（AE Mask、Resolve Power Window）
  - 用一个形状限制图形或画面效果的显示区域，做"揭示"类转场。
- [ ] **光源图形**
  - 带颜色和强度的点光源，配合光影包或自定义光照。

---

## 三、时间轴界面

### P1
- [ ] **入点 / 出点与循环播放**（I / O 键）
  - 设定一段区间循环预览。
- [ ] **播放快捷键 J / K / L**（Premiere、Resolve）
  - 倒放、暂停、快进，多按加速；左右方向键逐帧。
- [ ] **轨道管理**（Premiere、Resolve）
  - 轨道锁定、单独显示（Solo）、折叠、轨道文件夹。
  - 已完成一部分：轨道点选、框选、多条一起拖动、Del 删除。
- [ ] **拖动时的数值提示**
  - 拖动关键帧时在鼠标旁显示"+12 tick / 第 340 tick"，缩放时显示倍率。
- [ ] **时间码显示与跳转**
  - 在秒、tick、帧之间切换显示单位，支持输入时间跳转。

### P2
- [ ] **波纹编辑（Ripple）**（Premiere、Resolve）
  - 插入或删除一段时间时，后面所有关键帧整体平移。
- [ ] **历史面板**（Photoshop、Blender Undo History）
  - 把撤销栈列成列表，点任意一条回到那一步。
- [ ] **关键帧颜色标签**
  - 给关键帧染色、加小标签，方便辨认。

---

## 四、属性面板（检查器）

### P1
- [ ] **拖动标签改值**（AE、Unity、Blender）
  - 按住数值字段的标签左右拖动改值。
- [ ] **重置为默认值**（Unity、Blender）
  - 右键条目选"重置"，复用条目识别机制：采集默认关键帧，再用重放写回。
- [ ] **表达式输入**（Blender、AE）
  - 数值框里输入 `=2*3`、`+0.5`（在当前值上加）。

### P2
- [ ] **属性预设**（AE Animation Presets、Unity Presets）
  - 把一组条目的值存成具名预设一键套用，在属性剪贴板基础上扩展成多个具名剪贴板。
- [ ] **属性搜索**
  - 属性窗口顶部搜索框，按名称过滤条目。

---

## 五、视口

### P1
- [ ] **相机预览小窗（画中画）**（Blender 相机视图、Resolve Viewer）
  - 用编辑器相机查看场景时，角落显示成片相机此刻的画面。
- [ ] **相机视锥可视化**
  - 在相机关键帧和环绕关键帧的机位上画出视锥（受 FOV 和画面比例影响）。
- [x] **复制图形（Ctrl+D）**
  - 复制选中的图形连同关键帧到新轨道。
  - [ ] 复制后立刻进入移动模式。

### P2
- [ ] **对齐与分布**（Figma、Blender）
  - 多个图形对齐、居中、等距分布，基于批量 Gizmo 的共同中心计算。
- [ ] **阵列 / 重复**（C4D Cloner、Blender Array）
  - 按直线、圆形或网格重复 N 份，可依次错开出场时间。
- [ ] **测量工具**
  - 在视口里量两点的距离和角度。

---

## 六、工作流与易用性

### P1
- [ ] **快捷键速查表**
  - 按 `?` 或从菜单打开面板，列出 G/R/B/M、Ctrl 吸附、Ctrl+右键选择、中键放置、Ctrl+B 切分等快捷键。
- [ ] **空状态引导**
  - 属性窗口、Shape Manager、预制篮为空时提示下一步，比如"在时间轴上右键新建 Shape 轨道"。
- [ ] **预制导出 / 导入**
  - 把预制导出成单个文件分享，再从文件导入。

### P2
- [ ] **低分辨率预览导出**（Premiere 代理、Resolve Render in Place）
  - 低分辨率、低帧率快速导出小样检查节奏。
- [ ] **多段导出 / 渲染队列**（Media Encoder、Resolve Render Queue）
  - 一次排队导出多个区间或多种设置，比如竖屏和横屏各一版。
- [ ] **自定义快捷键**
  - vector3 的快捷键加入 Flashback 的快捷键设置界面，可重新绑定。

---

## 建议的实施顺序
1. **马上能提升手感的**：一键缓动、时间轴吸附、入点出点循环播放、拖动时的数值提示、快捷键速查表。
2. **补上核心能力的**：图表编辑器、运动路径可视化、路径跟随、转场、相机预览小窗。
3. **放大创作空间的**：镜头切换轨、调色和景深、文字动画器、阵列。
4. **长期架构项**：按属性分别打关键帧、驱动和表达式。
